import { makeStyles, Box, Typography } from "@material-ui/core";
const Sidebar = ({}) => {
  const styles = useStyles();
  return (
    <Box>
      <Box>
        <Typography>Main Feed</Typography>
      </Box>
    </Box>
  );
};
export default Sidebar;
const useStyles = makeStyles((theme) => ({}));
